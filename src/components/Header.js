import React from 'react';

const Header = () => {
    return (
        <div className="header">
            <img className="logo" />
            <button className="header__btn">Start Slideshow</button>
        </div>
    )
}

export default Header